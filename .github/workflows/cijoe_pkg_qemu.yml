---
name: cijoe_pkg_qemu

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: cijoe_pkg_qemu
    tags:
    - "v*"

defaults:
  run:
    shell: bash

jobs:

  qemu:
    runs-on: ${{ matrix.os }}

    container:
      image: ghcr.io/refenv/cijoe-docker:latest
      options: --privileged

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: ['3.12']
        usage_example: ['qemu.build']

    steps:
    - name: Grab source
      uses: actions/checkout@v4.2.2
      with:
        path: cijoe

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5.3.0
      with:
        python-version: ${{ matrix.python-version }}

    - name: Build and install cijoe from source
      run: |
        pipx uninstall cijoe
        cd cijoe
        make deps info build install
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Emit the example configuration file and workflow
      run: |
        cijoe --example ${{ matrix.usage_example }}

    - name: Run it!
      env:
        LIBGUESTFS_BACKEND: direct
      run: |
        cijoe --monitor -l

    - name: Upload report
      if: always()
      uses: actions/upload-artifact@v4.3.0
      with:
        name: report-cijoe_pkg_qemu-${{ matrix.usage_example }}
        path: cijoe-output/*
        if-no-files-found: error
